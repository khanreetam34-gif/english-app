# Private-use build

This rebuild is intended for the user's own testing only.

It is not configured for Play Store publishing. The target remains a normal Android
application that can be installed into a supported purchased cloud Android device.

The original APK's private/native/server components are not represented as recovered
source. Only functionality that can be legitimately reconstructed from the supplied
application/reference is included.
