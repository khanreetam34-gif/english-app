# v12

The centralized app state is now wired into the native UI:
- Settings switches persist for the current app session.
- HUD visibility updates app state.
- Cloud connection updates connection state.
- English action labels remain centralized.
- The app remains a normal Android APK intended to run inside supported cloud Android phones.

Unavailable proprietary/native functionality is still not fabricated.
