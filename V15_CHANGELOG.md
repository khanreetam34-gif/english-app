# v15

This build moves the rebuild closer to a usable private app:
- Settings are persisted across app restarts with SharedPreferences.
- HUD visibility is persisted.
- Notification, floating-window and auto-start preferences are persisted.
- The project remains a normal Android APK target for supported cloud Android phones.
- No separate cloud-phone service is included.
