# v38 Local State

The rebuild's supported local state is:
- App settings
- HUD state
- Runtime event/log history
- Feature readiness state

No passwords, tokens, private backend credentials, or proprietary runtime data are exported.

For a future authorized backend, this local boundary can be replaced by a documented API layer without changing the UI contract.
