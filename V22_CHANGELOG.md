# v22

Added a real local OCR engine integration using Google ML Kit's Latin text-recognition API.

The rebuild can now recognize English text from a selected bitmap when the dependency is available.

Script execution is intentionally not enabled from the recovered `script.lr` asset; the file remains available for inspection/authorized runtime work.
